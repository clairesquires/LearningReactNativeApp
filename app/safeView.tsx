import { SafeAreaView } from 'react-native';
import { OverflowingScreen } from '../src/screens/OverflowingScreen';

export default function SafeViewPage() {
  return (
    <SafeAreaView>
      <OverflowingScreen />
    </SafeAreaView>
  );
}
